# Generated by shaka seam migrate.
---
version: 1
base_branch: main
review:
  # No CI review report is required; AGENTS.md still requires the full check, thread, and review-app gate.
  required: none
merge:
  preference: ask
repo_prefix: RORD
